

const HomeScreen = () => {
  return (
      <div style={{  display:'flex',
                     flexDirection: 'column',
                     margin:25,
                     borderColor:"black",
                     padding:10,
                     justifyContent: 'space-around', }}>
        <div style={{fontSize:40}}>
          Welcome to the BBoard App
        </div>
    </div>
  );
};

export default HomeScreen;
